# Lee DevKit 依赖包
# 核心依赖 - 仅包含必要的包以保持轻量级

# 命令行参数解析（Python 3.7+ 内置，但明确声明以确保兼容性）
argparse

# 开发依赖（可选）
# 如果需要开发环境，可以取消注释以下包：
# pytest>=6.0.0
# pytest-cov>=2.10.0
# black>=21.0.0
# flake8>=3.8.0
# mypy>=0.800